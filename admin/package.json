{
  "name": "photocopie-admin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute photocopie-admin --local --file=./migrations/0001_init.sql && wrangler d1 execute photocopie-admin --local --file=./migrations/0002_codes_installation.sql",
    "db:migrate:remote": "wrangler d1 execute photocopie-admin --remote --file=./migrations/0001_init.sql && wrangler d1 execute photocopie-admin --remote --file=./migrations/0002_codes_installation.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "cloudflare": {
    "bindings": {
      "ADMIN_PASSWORD": {
        "description": "Le mot de passe que tu veux utiliser pour te connecter à ton tableau de bord."
      },
      "CLE_PRIVEE_LICENCE": {
        "description": "Clé privée Ed25519 (64 caractères hexadécimaux) qui signe les licences. La clé publique correspondante est dans src-tauri/src/license.rs — jamais l'inverse."
      }
    }
  }
}
